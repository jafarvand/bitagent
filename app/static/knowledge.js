const kb = id => document.getElementById(id);
const today = new Date(); const expiry = new Date(today); expiry.setFullYear(expiry.getFullYear() + 1);
kb("kb-effective").value = today.toISOString().slice(0, 10); kb("kb-expires").value = expiry.toISOString().slice(0, 10);
kb("kb-file").addEventListener("change", async event => { const file = event.target.files[0]; if (!file) return; kb("kb-content").value = await file.text(); if (!kb("kb-title").value) kb("kb-title").value = file.name.replace(/\.[^.]+$/, ""); if (!kb("kb-document-id").value) kb("kb-document-id").value = file.name.toLowerCase().replace(/\.[^.]+$/, "").replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, ""); });
const list = id => kb(id).value.split(",").map(value => value.trim()).filter(Boolean);
kb("knowledge-form").addEventListener("submit", async event => { event.preventDefault(); kb("kb-process").disabled = true; kb("knowledge-state").textContent = "processing"; const status = kb("kb-status").value; const body = {tenant_id:kb("kb-tenant").value.trim(),document_id:kb("kb-document-id").value.trim(),title:kb("kb-title").value.trim(),document_type:kb("kb-type").value,version:kb("kb-version").value.trim(),owner:kb("kb-owner").value.trim(),approval_status:status,approved_by_role:status === "approved" ? kb("kb-approver").value.trim() : null,effective_at:`${kb("kb-effective").value}T00:00:00Z`,expires_at:`${kb("kb-expires").value}T23:59:59Z`,data_class:kb("kb-class").value,allowed_roles:list("kb-roles"),keywords:list("kb-keywords"),content:kb("kb-content").value,source_ref:kb("kb-source").value.trim()}; try { const response = await fetch("/api/v0/xima/knowledge/documents",{method:"POST",headers:{"Content-Type":"application/json","X-BitAgent-Role":"admin"},body:JSON.stringify(body)}); const payload = await response.json(); if (!response.ok) throw new Error(JSON.stringify(payload.detail || payload)); kb("kb-result").textContent = JSON.stringify(payload.document,null,2); kb("knowledge-state").textContent = "processed"; kb("knowledge-state").className = "pill good"; kb("qa-question").focus(); } catch(error) { kb("kb-result").textContent = error.message; kb("knowledge-state").textContent = "failed"; kb("knowledge-state").className = "pill warn"; } finally { kb("kb-process").disabled = false; } });
kb("qa-form").addEventListener("submit", async event => { event.preventDefault(); kb("qa-state").textContent = "testing"; try { const response = await fetch("/api/v0/xima/knowledge/qa",{method:"POST",headers:{"Content-Type":"application/json","X-BitAgent-Role":"operator"},body:JSON.stringify({tenant_id:kb("kb-tenant").value.trim(),question:kb("qa-question").value.trim()})}); const payload = await response.json(); if (!response.ok) throw new Error(JSON.stringify(payload.detail || payload)); const result = payload.result; kb("qa-answer").textContent = result.answer || result.limitations.join(" "); kb("qa-state").textContent = `${result.status} · ${result.citations.length} citation(s)`; kb("qa-state").className = result.answer ? "pill good" : "pill warn"; } catch(error) { kb("qa-answer").textContent = error.message; kb("qa-state").textContent = "failed"; kb("qa-state").className = "pill warn"; } });
